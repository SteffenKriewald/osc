\name{osc.buffer}
\alias{osc.buffer}
\title{
Simple Buffer algorithm
}
\description{
Simple buffer based on euclidean distance are created around all cells equal to one.
}
\usage{
osc.buffer(input, width) 
}
\arguments{
  \item{input}{
    Matrix or Raster containing 1 idicating a cluster, no NA values are allowed
}
  \item{width}{
    Dataframe with additional data, containing at least a lat and long coloumn with point coordinates which will be assigned.
}
}

\value{
 Returns matrix or raster, depending on input, with 1 for cluster and -1 for surrounding buffer
}

\examples{
data("landcover")

landcover <- landcover*0
landcover[is.na(landcover)] <- 1

plot(osc.buffer(landcover, 10))
}
\keyword{utils}
